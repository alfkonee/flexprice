{{- if .Values.flexprice.ingress.enabled }}
{{- $fullName := include "flexprice.fullname" . -}}
{{- $svcPort := .Values.flexprice.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "flexprice.labels" . | nindent 4 }}
  {{- with .Values.flexprice.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.flexprice.ingress.className }}
  ingressClassName: {{ .Values.flexprice.ingress.className }}
  {{- end }}
  {{- if .Values.flexprice.ingress.tls }}
  tls:
    {{- range .Values.flexprice.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.flexprice.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}-api
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
